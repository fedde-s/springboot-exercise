server.port=8090

# enable fine-grained logging
logging.level.nl.thehyve.fedde.springcourse=DEBUG
logging.file=hobby-application.log

spring.datasource.url=jdbc:mysql://localhost:8307/hobbydb
spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.username=hobby_user
spring.datasource.password=resu_ybboh
spring.jpa.database_platform=org.hibernate.dialect.MySQL5Dialect

# TODO: uncomment after designing schema
spring.jpa.hibernate.ddl-auto=update

# show the sql so I know what happens around here
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# prevent mysql/jpa timeout issue each hour (3600000 millis) poll the DB
spring.datasource.testWhileIdle=true
spring.datasource.timeBetweenEvictionRunsMillis=3600000
spring.datasource.validationQuery=SELECT 1
